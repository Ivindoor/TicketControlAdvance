package com.sanmina.tk.orm.dao.skid;

import com.sanmina.tk.orm.model.skid.Container;

public interface ContainerDao {
	
	void saveContainer(Container container);

	void updateContainer(Container container);
	
	void  deleteContainer(Container container);
	
	Container findContainerByContainer(Long container);

}
